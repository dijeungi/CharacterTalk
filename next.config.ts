// next.config.ts
// Next.js 프로젝트 전역 설정 파일
// 기본적으로 건드리지 않아도 되며, 필요 시 옵션 추가

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // 예: 이미지 도메인 설정, 리다이렉트, 환경변수 공개 등 추가 가능
};

export default nextConfig;
